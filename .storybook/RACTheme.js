import { create } from '@storybook/theming'
import Logo from '../stylesheets/assets/img/logo.svg'

export default create({
  base: 'light',
  brandTitle: 'Pitt Branding applying over RACs',
  brandImage: Logo
})
